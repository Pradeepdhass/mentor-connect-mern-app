PORT=4000
# Replace with your MongoDB Atlas connection string
MONGO_URI=mongodb+srv://<username>:<password>@<cluster-url>/MentorConnect?retryWrites=true&w=majority
# Frontend origin allowed by CORS (update after frontend deploy)
CORS_ORIGIN=http://localhost:5173